# ── 嵌套的独立代码仓（外层不追踪，各自推各自的远端，坐标见 repos.json）──
/platform/

# ── 选型阶段拉来研究的第三方开源项目（只读参考，不是业务代码，不进版本库）──
/vendor/

# ── 本机会话产物 / agent 注入（都是本机路径，不进版本库）──
.claude/
CLAUDE.local.md
AGENTS.md

# ── 系统与工具杂物 ──
.DS_Store
__pycache__/
*.pyc
.venv/
